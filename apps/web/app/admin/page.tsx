import { companyStatusAction, createCompanyAction, logoutAction } from "@/app/actions";
import { Flash } from "@/components/flash";
import { StatusBadge } from "@/components/status-badge";
import { requirePlatformAdmin } from "@/lib/auth";
import { money } from "@/lib/format";
import { listCompaniesForAdmin } from "@/lib/services";
export const dynamic="force-dynamic";
export default async function AdminPage({searchParams}:{searchParams:Promise<{success?:string;error?:string}>}){const admin=await requirePlatformAdmin();const companies=listCompaniesForAdmin();const flash=await searchParams;return <main className="content" style={{maxWidth:1400}}><div className="flex between center mb"><div className="logo" style={{padding:0,color:"#172126"}}><span className="logo-mark">M</span><span>Platform administration</span></div><form action={logoutAction}><button className="btn secondary">Sign out {admin.name}</button></form></div><Flash {...flash}/><div className="grid two-col"><section className="card"><div className="card-head"><h3>Contracted delivery companies</h3></div><div className="table-wrap"><table className="table"><thead><tr><th>Company</th><th>Usage</th><th>Contract</th><th>Status</th><th>Action</th></tr></thead><tbody>{companies.map((c:any)=><tr key={c.id}><td><b>{c.name}</b><div className="muted small">@{c.handle}</div></td><td>{c.shipment_count} shipments<div className="muted small">{c.member_count} team users</div></td><td>{c.contract_model||"—"}<div className="muted small">{money(c.contract_amount,c.contract_currency||"ETB")}</div></td><td><StatusBadge value={c.status}/></td><td><form action={companyStatusAction} className="stack"><input type="hidden" name="companyId" value={c.id}/><input type="hidden" name="status" value={c.status==="ACTIVE"?"SUSPENDED":"ACTIVE"}/><input className="input" name="reason" placeholder="Reason" required/><button className={`btn small ${c.status==="ACTIVE"?"danger":""}`}>{c.status==="ACTIVE"?"Suspend":"Activate"}</button></form></td></tr>)}</tbody></table></div></section><section className="card"><div className="card-head"><h3>Create company and owner</h3></div><form className="card-body form-grid" action={createCompanyAction}><div className="field"><label>Company name</label><input className="input" name="name" required/></div><div className="field"><label>Public handle</label><input className="input" name="handle" placeholder="companyname" required/></div><div className="field"><label>Company phone</label><input className="input" name="phone" required/></div><div className="field"><label>Company email</label><input className="input" type="email" name="email"/></div><div className="field"><label>Owner name</label><input className="input" name="ownerName" required/></div><div className="field"><label>Owner email</label><input className="input" type="email" name="ownerEmail" required/></div><div className="field"><label>Owner phone</label><input className="input" name="ownerPhone"/></div><div className="field"><label>Temporary password</label><input className="input" type="password" name="temporaryPassword" minLength={10} required/></div><button className="btn full">Create company workspace</button></form></section></div></main>}
