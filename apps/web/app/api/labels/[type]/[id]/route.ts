import QRCode from "qrcode";
import { NextResponse } from "next/server";
import { requireMember } from "@/lib/auth";
import { getLabelData } from "@/lib/services";
import { maskPhone } from "@/lib/format";
const esc=(v:unknown)=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&apos;"}[c]||c));
export async function GET(request:Request,{params}:{params:Promise<{type:string;id:string}>}){try{const ctx=await requireMember();const {type,id}=await params;if(type!=="shipment"&&type!=="batch")return NextResponse.json({error:"Invalid label type."},{status:400});const d=getLabelData(ctx,type,id);const qr=await QRCode.toDataURL(d.qrToken,{margin:1,width:520});const shipment=type==="shipment";const body=shipment?`<text x="28" y="70" class="big">${esc(d.tracking_number)}</text><image href="${qr}" x="26" y="90" width="210" height="210"/><rect x="250" y="95" width="310" height="78"/><text x="265" y="128" class="label">FINAL</text><text x="265" y="158" class="route">${esc(d.destination_code||d.destination_label)}</text><rect x="250" y="185" width="310" height="78"/><text x="265" y="218" class="label">NEXT</text><text x="265" y="248" class="route">${esc(d.next_route_code||d.next_destination||"CUSTOMER HANDOFF")}</text><text x="28" y="335" class="label">SENDER</text><text x="28" y="360" class="text">${esc(d.sender_name)} · ${esc(maskPhone(d.sender_phone))}</text><text x="28" y="400" class="label">RECEIVER</text><text x="28" y="425" class="text">${esc(d.receiver_name)} · ${esc(maskPhone(d.receiver_phone))}</text><text x="28" y="465" class="label">ORIGIN</text><text x="28" y="490" class="text">${esc(d.origin_label)}</text><text x="28" y="530" class="label">DESTINATION</text><text x="28" y="555" class="text">${esc(d.destination_label)}</text><text x="28" y="595" class="label">CONTENTS</text><text x="28" y="620" class="text">${esc(d.contents)}</text>`:`<text x="28" y="70" class="big">${esc(d.batch_number)}</text><image href="${qr}" x="26" y="100" width="230" height="230"/><text x="280" y="140" class="label">ROUTE LEG</text><text x="280" y="175" class="route">${esc(d.route_code)}</text><text x="280" y="225" class="text">${esc(d.origin_name)}</text><text x="280" y="260" class="big">↓</text><text x="280" y="310" class="text">${esc(d.destination_name)}</text><text x="28" y="390" class="label">SHIPMENTS</text><text x="28" y="430" class="big">${esc(d.shipment_count)}</text><text x="260" y="390" class="label">STATUS</text><text x="260" y="430" class="route">${esc(d.state)}</text><text x="28" y="500" class="label">EXPECTED DEPARTURE</text><text x="28" y="530" class="text">${esc(d.expected_departure||"Not set")}</text><text x="28" y="580" class="label">EXPECTED ARRIVAL</text><text x="28" y="610" class="text">${esc(d.expected_arrival||"Not set")}</text>`;const svg=`<svg xmlns="http://www.w3.org/2000/svg" width="4in" height="6in" viewBox="0 0 600 900"><style>.big{font:700 30px system-ui,sans-serif}.route{font:700 22px system-ui,sans-serif}.text{font:18px system-ui,sans-serif}.label{font:700 13px system-ui,sans-serif;letter-spacing:1px}</style><rect width="600" height="900" fill="white"/><text x="28" y="35" class="route">${esc(d.company_name)}</text><line x1="28" x2="572" y1="48" y2="48" stroke="black" stroke-width="3"/>${body}<text x="28" y="860" class="label">QR CONTAINS AN OPAQUE IDENTIFIER ONLY</text></svg>`;const url=new URL(request.url);const filename=(url.searchParams.get("filename")||`${type}-${id}.svg`).replace(/[^\p{L}\p{N}._-]+/gu,"-").slice(0,180);return new NextResponse(svg,{headers:{"Content-Type":"image/svg+xml; charset=utf-8","Content-Disposition":`attachment; filename*=UTF-8''${encodeURIComponent(filename)}`,"Cache-Control":"no-store"}});}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Unauthorized"},{status:401});}}
